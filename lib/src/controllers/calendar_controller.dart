import 'package:get/get.dart';

class CalendarController extends GetxController {
  // final Rx<CalendarFormat> cFormat = CalendarFormat.month.obs;
  // final DateTime today = DateTime.now();
  // final Rx<DateTime> fDay = DateTime.now().obs;
  // final Rx<DateTime> sDay = DateTime.now().obs;

  // DateTime get selectedDay => sDay.value;
  // DateTime get focusedDay => fDay.value;
  // CalendarFormat get calendarFormat => cFormat.value;

  // DateTime get firstDay => DateTime(today.year - 1, today.month, today.day);
  // DateTime get lastDay => DateTime(today.year + 1, today.month, today.day);

  // set selectedDay(DateTime value) {
  //   sDay.value = value;
  //   update();
  // }

  // set focusedDay(DateTime value) {
  //   fDay.value = value;
  //   update();
  // }

  // set calendarFormat(CalendarFormat value) {
  //   cFormat.value = value;
  //   update();
  // }
}
